using UnityEngine;

public class InstantiateChild : MonoBehaviour
{
    [SerializeField] GameObject playerChild;

   
    void Start()
    {
       // Instantiate(playerChild, gameObject.transform);
    }
}
